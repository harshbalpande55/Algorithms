def selectionsort(l):
    for start in range(len(l)):
        minpos = start
        for i in range(start,len(l)):
            if l[minpos] > l[i] :
                minpos = i
        l[start],l[minpos]=l[minpos],l[start]
    print(l)
l=[9,8,7,4,5,6,1,2,3]
selectionsort(l)
